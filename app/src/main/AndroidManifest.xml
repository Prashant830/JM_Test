<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.prashant830.jmtest">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application

        android:name=".core.di.HiltInstance"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JmTest"
        tools:targetApi="31">
        <activity
            android:name=".core.uimvvmlayer.secondui.CategoryIcoActivity"
            android:exported="false" />
        <activity
            android:name=".core.uimvvmlayer.thirdui.SearchActivity"
            android:exported="false" />

        <activity
            android:name=".core.uimvvmlayer.firstui.SplaceActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>